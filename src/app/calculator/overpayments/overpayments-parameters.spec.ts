import { OverpaymentsParameters } from './overpayments-parameters';

describe('OverpaymentsParameters', () => {
  it('should create an instance', () => {
    expect(new OverpaymentsParameters()).toBeTruthy();
  });
});
