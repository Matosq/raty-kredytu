import { TranchesParameters } from './tranches-parameters';

describe('TranchesParameters', () => {
  it('should create an instance', () => {
    expect(new TranchesParameters()).toBeTruthy();
  });
});
